import React from 'react';

import AppContainer from './app/components/navigation/AppNavigation';

export default class App extends React.Component {
  state = { };

  render() {
    return (
      <AppContainer />
    );
  }
}
